---
interface Props {
  figcaption: string;
}
const { figcaption } = Astro.props;
---

<figure class="flex flex-col items-center gap-1">
  <slot />
  <figcaption class="hidden md:block text-sm text-black/80">
    {figcaption}
  </figcaption>
</figure>
