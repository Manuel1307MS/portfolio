---
import { Image } from "astro:assets";
interface Props {
  src: ImageMetadata;
  alt: string;
}
const { src, alt } = Astro.props;
---

<div class="flex max-w-225">
  <Image src={src} alt={alt} width={900} height={600} layout="constrained" />
</div>
